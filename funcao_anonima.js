// Uma função anônima em JavaScript é uma função sem nome, geralmente usada quando você precisa de uma função temporária. Ela pode ser atribuída a uma variável ou passada como argumento.

const soma = function(a, b) {
  return a + b;
};

// Essa função não tem nome, mas pode ser chamada pela variável `soma`.